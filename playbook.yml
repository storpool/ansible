#!/usr/bin/env ansible-playbook

- name: "Deploy Storpool"
  gather_facts: True
  hosts: storpool
  become: yes
  tasks:
    - import_role:
        name: storpool
  tags:
    - DEPLOY_STORPOOL

- name: "Reboot servers"
  import_playbook: reboot.yml
  tags:
    - REBOOT_HOSTS
